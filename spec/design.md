---
name: english-conversation-assistant-design
description: 英文對話助手（ECA）技術設計 — 即時翻譯 + 通話前準備 + Quick Response Bar + Smart 建議 + 場景預設講稿
status: approved
version: 2.8
date: 2026-02-07
---

# English Conversation Assistant — Design v2.0

## 1. 產品定位

> **「聽懂對方說什麼，知道自己怎麼說」**
> —— 讓英文電話不再焦慮的即時輔助工具

### 1.1 核心差異化

| 功能需求 | 翻譯類 | 面試助手 | 語言學習 | **我們** |
|---------|--------|----------|---------|---------|
| 即時翻譯（英→中） | ✅ | ❌ | ❌ | ✅ |
| 講稿生成（中→英） | ❌ | ❌ | ❌ | **✅ ★** |
| 情境回應建議 | ❌ | ✅ | ❌ | **✅ ★** |
| 電話場景優化 | ❌ | ❌ | ❌ | **✅ ★** |
| 用戶主導（自己說） | N/A | ❌ | ✅ | **✅ ★** |

### 1.2 目標用戶

**主要用戶**：香港 BNO 移民在英國（150,400+ 人）

**核心痛點**：
- 66% 非母語者有電話焦慮
- 打電話給 GP/銀行/Council 是最大障礙
- 現有解決方案不足：Google Translate 無情境理解、AI Call 用戶失控

---

## 2. 系統架構

### 2.1 架構概覽

```
┌─────────────────────────────────────────────────────────────┐
│                         Frontend                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Web App: React + Tailwind                           │    │
│  │ Desktop: Tauri v2 (Phase 2)                         │    │
│  └─────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────┤
│                         Backend                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ API Server: FastAPI (Python)                        │    │
│  │ WebRTC Gateway: Browser native                      │    │
│  └─────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────┤
│                       AI Services                            │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ STT + 翻譯: OpenAI Realtime API (gpt-realtime-mini) │    │
│  │ 講稿生成 + 建議: gpt-5-mini (Responses API)         │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技術棧

| 層級 | 選型 | 備註 |
|------|------|------|
| **前端** | Vanilla JS (ES6+) → React (Phase 2) | MVP 先用 Vanilla JS |
| **樣式** | CSS3 + Tailwind (Phase 2) | 繁體中文字體：Noto Sans TC |
| **後端** | FastAPI (Python 3.10+) | 異步支援 |
| **STT/翻譯** | `gpt-realtime-mini` | WebRTC 即時串流 |
| **講稿/建議** | `gpt-5-mini` | Responses API |
| **資料庫** | localStorage (MVP) → PostgreSQL (v2) | 對話記錄 |

### 2.3 模型硬性規則

🚨 **此規則優先級最高，不可變更**：
- **Realtime 語音**：必須使用 `gpt-realtime-mini`
- **文字控制器**：必須使用 `gpt-5-mini`（講稿生成、Smart 建議）
- **即時翻譯**：必須使用 `gpt-4.1-nano`（經測試為最快模型，703ms 首字回應）

---

## 3. 核心功能設計

### 3.1 功能架構

```
┌─────────────────────────────────────────────────────────────┐
│               English Conversation Assistant                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  │
│  │   即時翻譯     │  │   講稿生成     │  │  Smart 建議   │  │
│  │  (Always On)  │  │  (On Demand)  │  │ (Smart Mode)  │  │
│  └───────┬───────┘  └───────┬───────┘  └───────┬───────┘  │
│          │                  │                  │           │
│  對方英文 → 中文字幕  用戶中文 → 英文講稿   問題 → 建議回應    │
│                                                             │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  │
│  │   快捷短語     │  │  Panic Button │  │   對話記錄    │  │
│  │  (One Tap)    │  │  (Emergency)  │  │   (Review)    │  │
│  └───────────────┘  └───────────────┘  └───────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 功能優先級

| 優先級 | 功能 | MVP | v1.5 | v2.0 |
|--------|------|-----|------|------|
| **P0** | 即時翻譯（英→中） | ✅ | ✅ | ✅ |
| **P0** | 講稿生成（中→英） | ✅ | ✅ | ✅ |
| **P0** | Panic Button | ✅ | ✅ | ✅ |
| **P1** | 快捷短語 | ✅ | ✅ | ✅ |
| **P1** | Smart 回應建議 | - | ✅ | ✅ |
| **P2** | 場景預設 | - | ✅ | ✅ |
| **P2** | 對話記錄 | - | ✅ | ✅ |
| **P2** | 信心指示 | - | - | ✅ |
| **P3** | Whisper Mode | - | - | ✅ |

---

## 4. 即時翻譯模組

### 4.1 雙軌音訊架構（Dual-Track Audio）

> **關鍵設計決策**：英文即時顯示使用 Web Speech API（本地處理，快），翻譯使用 OpenAI Realtime API。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Dual-Track Audio Architecture                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│     麥克風（擴音模式）                                                      │
│            │                                                             │
│            ├──────────────────────┬──────────────────────────┐          │
│            ▼                      ▼                          │          │
│   ┌─────────────────┐    ┌─────────────────┐                │          │
│   │  Web Speech API │    │  OpenAI Realtime │                │          │
│   │  (Browser STT)  │    │  (gpt-realtime-  │                │          │
│   │                 │    │   mini)          │                │          │
│   └────────┬────────┘    └────────┬─────────┘                │          │
│            │                      │                          │          │
│            ▼                      ▼                          │          │
│   ┌─────────────────┐    ┌─────────────────┐                │          │
│   │ 英文即時預覽     │    │ 英文最終轉錄     │                │          │
│   │ (~100ms 延遲)   │    │ + 中文翻譯       │                │          │
│   │ 本地處理，無成本 │    │ (~500ms 延遲)   │                │          │
│   └────────┬────────┘    └────────┬─────────┘                │          │
│            │                      │                          │          │
│            ▼                      ▼                          │          │
│   ┌─────────────────────────────────────────────────────┐   │          │
│   │                   SmartSegmenter                     │   │          │
│   │              (智能分段 + 並行翻譯管理)                  │   │          │
│   └─────────────────────────────────────────────────────┘   │          │
│                              │                                           │
│                              ▼                                           │
│                    ┌─────────────────┐                                  │
│                    │    UI 顯示      │                                  │
│                    │  (最新在上)     │                                  │
│                    └─────────────────┘                                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

**選擇 Web Speech API 的原因**：
- 本地處理，延遲極低（~100ms）
- 無 API 成本
- 用戶可即時看到對方說的英文

**方案 A: 兩階段架構**（已採用）：
- Web Speech API → SmartSegmenter → `/api/translate/stream`（gpt-4.1-nano）
- 不使用 OpenAI Realtime API 翻譯（會進入 Q&A 對話模式）
- 翻譯使用 SSE 串流，首字回應 ~700ms

**OpenAI Realtime API 的角色**（僅用於 STT）：
- 提供最終轉錄結果（比 Web Speech 更準確）
- 提供語義 VAD（semantic_vad）用於分段信號
- **不用於翻譯**（詳見 lessons_learned.md §方案 A）

### 4.2 智能分段系統（SmartSegmenter）

> **核心問題**：Web Speech API 不會智能分段，等待用戶完全停止說話（>1-2秒）才觸發，導致翻譯延遲過長。

> ✅ **實現狀態（2026-02-02）**：
> - `src/frontend/smart_segmenter.js` - SmartSegmenter + AdaptiveSegmenter 類
> - 整合至 `eca_parallel_test.html`
> - 禁用 OpenAI `create_response: true`，改由前端控制分段
> - 使用 `input_audio_buffer.commit` + `response.create` 強制翻譯

#### 4.2.1 人類說話習慣參數

| 參數 | 數值 | 來源 |
|------|------|------|
| 電話客服語速 | 140-160 WPM | VirtualSpeech, The Speaker Lab |
| 句內停頓（逗號） | ~490ms (±260ms) | BYU Scholars Archive |
| 句間停頓（句號） | ~1010ms (±400ms) | BYU Scholars Archive |
| 最佳理解語速 | 150-160 WPM | 密蘇里大學研究 |

#### 4.2.2 混合分段策略（Hybrid Segmentation）

```
┌─────────────────────────────────────────────────────────────┐
│                    輸入：連續語音文字流                        │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Stage 1: 時間閾值檢測                                       │
│  - 停頓 > 600ms → 可能是句子邊界                             │
│  - 停頓 > 300ms → 可能是子句邊界                             │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Stage 2: 語法線索檢測                                       │
│  - 句末詞彙：right?, okay, thanks, please                    │
│  - 問句結構：do you, can you, is it, what, where, when      │
│  - 連接詞：and, but, or, so, because                        │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│  Stage 3: 長度保護                                           │
│  - 超過 15 字且有子句邊界 → 觸發分段                          │
│  - 超過 25 字 → 強制分段（無論是否有邊界）                     │
└─────────────────────────────────────────────────────────────┘
```

#### 4.2.3 分段參數與預設模式

**5 種預設模式**（用戶可在 UI 選擇）：

| 模式 | pauseThreshold | stabilityDelay | softLimit | hardLimit | 適用場景 |
|------|---------------|----------------|-----------|-----------|---------|
| 🚀 極速 | 400ms | 80ms | 12 | 20 | 最快反應，可能切斷單詞 |
| ⚡ 快速 | 500ms | 100ms | 12 | 20 | **預設**，快速反應 |
| ⚖️ 平衡 | 600ms | 150ms | 15 | 25 | 平衡速度與穩定性 |
| 🛡️ 穩定 | 750ms | 200ms | 15 | 25 | 更穩定，較慢 |
| 🔒 保守 | 900ms | 250ms | 18 | 30 | 最穩定，最慢 |

```javascript
const SEGMENTER_PRESETS = {
  'fast': {  // 預設
    pauseThreshold: 500,
    stabilityDelay: 100,
    softLimit: 12,
    hardLimit: 20,
    minSegmentWords: 2
  }
  // ... 其他預設見 eca_parallel_test.html
};

const GRAMMAR_TRIGGERS = {
  sentence_enders: ['right', 'okay', 'ok', 'thanks', 'please', 'bye'],
  question_starters: ['do you', 'can you', 'is it', 'what', 'where', 'when', 'why', 'how'],
  conjunctions: ['and', 'but', 'or', 'so', 'because', 'however']
};
```

**動態穩定性檢測**：
- 當偵測到暫停時，不立即發出，而是等待 `stabilityDelay`
- 如果在等待期間有新文字進來，取消並重新等待
- 這樣可以動態處理任何內容，避免單詞中間切割

#### 4.2.4 語速自適應

| 語速估計 | pauseThreshold | softLimit | hardLimit |
|----------|----------------|-----------|-----------|
| 慢速 (<120 WPM) | 800ms | 12 words | 20 words |
| 正常 (120-160 WPM) | 600ms | 15 words | 25 words |
| 快速 (>160 WPM) | 500ms | 18 words | 30 words |

### 4.3 並行翻譯架構（Parallel Translation）

> **核心問題**：當新段落開始時，舊段落的翻譯會卡住，因為 `response.*` 事件不包含 `item_id`。

#### 4.3.1 Segment 數據結構

```javascript
class Segment {
    id: string;              // UI 顯示用 ID (seg-1, seg-2, ...)
    itemId: string;          // OpenAI item_id（主鍵）
    englishText: string;     // 英文原文
    chineseTranslation: string;  // 中文翻譯
    status: SegmentStatus;   // 狀態機
    responseId: string;      // 關聯的 response_id
    createdAt: number;       // 創建時間戳
    completedAt: number;     // 完成時間戳
}

// 狀態機
const SegmentStatus = {
    LISTENING: 'listening',       // 正在接收語音
    TRANSCRIBING: 'transcribing', // 等待轉錄結果
    TRANSLATING: 'translating',   // 正在翻譯
    DONE: 'done',                 // 完成
    ERROR: 'error'                // 錯誤
};
```

#### 4.3.2 SegmentStore（雙向索引）

```javascript
class SegmentStore {
    segments: Map<itemId, Segment>;           // item_id → Segment
    responseToSegment: Map<responseId, Segment>; // response_id → Segment
    pendingForResponse: [];                   // FIFO 隊列：等待 response 的 item_id
}
```

**關鍵設計**：
- 雙向索引：可從 `item_id` 或 `response_id` 找到 Segment
- 一旦關聯建立，不可覆蓋（防止競爭條件）
- FIFO 隊列處理 `response.created` 事件（因為它不包含 `item_id`）

#### 4.3.3 事件處理流程

```
事件序列：
1. speech_started (item_id: A)      → 創建 Segment A, 狀態=LISTENING
2. Web Speech onresult              → 更新 Segment A.englishText（即時預覽）
3. transcription.completed (A)      → Segment A 狀態=TRANSCRIBING, 加入 pendingQueue
4. speech_started (item_id: B)      → 創建 Segment B（與 A 獨立）
5. response.created (response_id: R1) → 從 pendingQueue 彈出 A, link(R1, A)
6. response.output_text.delta (R1)  → getByResponseId(R1) → Segment A, 追加翻譯
7. response.done (R1)               → Segment A 狀態=DONE

關鍵點：
- Segment A 和 B 的生命週期完全獨立
- B 的到來不會阻塞 A 的翻譯
- response_id 到 item_id 的映射通過 FIFO 隊列維護
```

#### 4.3.4 並行模型

```
Segment 1: ────────[listening]─[transcribing]─[translating]─[done]
Segment 2:               ────────[listening]─[transcribing]──────────[translating]─[done]
Segment 3:                             ────────[listening]─[transcribing]─[translating]─[done]

每個 Segment 的狀態轉換獨立進行，互不阻塞
```

> **📋 詳細實現規格**：完整的類實現代碼、事件處理器、UI 渲染器、錯誤處理策略見 [`design_parallel_translation.md`](./design_parallel_translation.md)

### 4.4 音訊捕獲

**採用方案**：麥克風 + 擴音模式

用戶將手機通話開啟擴音，電腦麥克風捕獲對方聲音。

**優點**：
- 操作簡單（一鍵開始）
- 跨平台支援（桌面 + 手機瀏覽器）
- 無需複雜權限（只需麥克風權限）

**使用流程**：
1. 用戶用手機撥打電話
2. 開啟擴音
3. 在電腦/手機瀏覽器開啟 ECA
4. 點擊「開始」，授權麥克風
5. 對方說話 → 即時翻譯顯示

> ~~**已移除**：`getDisplayMedia()` 系統音訊捕獲~~
> - 移除原因：主要場景（手機打電話）用不到
> - 日期：2026-02-04

### 4.5 翻譯延遲目標

| 指標 | 目標 | 方案 |
|------|------|------|
| 英文即時顯示 | <100ms | Web Speech API（本地） |
| 分段延遲 | <800ms | SmartSegmenter 混合策略 |
| 翻譯開始延遲 | <500ms | 句子結束後立即觸發 |
| 翻譯串流顯示 | 即時 | response.output_text.delta |
| 總端到端延遲 | <1.5s | 從說完到翻譯顯示 |

### 4.6 Session Instructions（翻譯模式）

```javascript
const translationInstructions = `
You are a real-time English to Traditional Chinese translator.

## Your Task
Listen to English speech and provide Traditional Chinese translation.

## Rules
1. Translate naturally, not word-by-word
2. Preserve the speaker's tone and intent
3. Use Hong Kong Traditional Chinese (繁體中文)
4. Keep translations concise for easy reading
5. If unsure about a word, provide best guess with [?] marker

## Output Format
- Only output the Chinese translation
- No explanations or meta-commentary
- One sentence at a time for real-time display
`;
```

### 4.7 Entry Management（字幕條目管理）

> **注意**：此節描述的是基於 `item_id` 的 Entry 管理，已整合到 4.3 節的 SegmentStore 架構中。SegmentStore 是 EntryState 的升級版本。

#### 4.7.1 Entry 定義（已整合至 Segment）

一個 **Entry** = 一段對方說的話 + 對應的中文翻譯

**Entry 邊界**：由 OpenAI 的 `item_id` 決定。每個 `item_id` 代表一個獨立的語音段。

**Entry 數據結構**：

```javascript
class Entry {
    id: string;           // UI 顯示用 ID (entry-1, entry-2, ...)
    itemId: string;       // OpenAI 的 item_id（主鍵）
    original: string;     // 英文原文
    translation: string;  // 中文翻譯
    isComplete: boolean;  // 是否完成
}

// 全局狀態
entryState = {
    entries: Map<itemId, Entry>,  // 所有 Entry，用 item_id 索引
    currentItemId: string | null, // 當前活躍的 item_id
    responseToItem: Map<responseId, itemId>, // response → item 對應
}
```

#### 4.7.2 事件來源與時序

| 來源 | 事件 | 特性 | 包含數據 |
|------|------|------|----------|
| Web Speech API | `onresult` | 本地處理，快（~100ms） | 累積的文字 |
| OpenAI Realtime | `speech_started` | 偵測到語音開始 | `item_id` |
| OpenAI Realtime | `transcription.completed` | 轉錄完成 | `item_id`, `transcript` |
| OpenAI Realtime | `response.output_text.delta` | 翻譯串流 | `delta` |
| OpenAI Realtime | `response.done` | 翻譯完成 | `response_id` |

**典型時序**：

```
時間線 →

用戶說話:     |====說 Hello====|  停頓  |====說 World====|

Web Speech:   [H] [He] [Hel] [Hello]      [W] [Wo] [World]
                   ↓ 快速、持續更新

OpenAI:                   [speech_started item_001]
                                         [transcription.completed]
                                              [response.delta 你好]
                                                   [response.done]
                                                        [speech_started item_002]
```

**關鍵觀察**：
1. Web Speech 比 OpenAI 快，用於即時預覽
2. Web Speech 會累積（每次 `onresult` 包含從 session 開始的所有文字）
3. OpenAI 用 `item_id` 分段
4. 事件可能亂序到達

#### 4.7.3 狀態轉換規則（已整合至 SegmentStore）

| 事件 | 動作 |
|------|------|
| `speech_started(item_id)` | 若 `item_id` 不存在，建立新 Entry；設 `currentItemId`；重啟 Web Speech |
| `Web Speech onresult` | 更新 `currentItemId` 對應 Entry 的 original |
| `transcription.completed(item_id)` | 用 `item_id` 找到 Entry，設 original（校正） |
| `response.created(response_id)` | 記錄 `responseToItem[response_id] = currentItemId` |
| `response.delta(response_id, delta)` | 用 `responseToItem` 找到 Entry，追加 translation |
| `response.done(response_id)` | 標記 Entry 為 complete |

#### 4.7.4 關鍵問題與解決方案（已整合至 4.3 節）

**問題 1：Web Speech 累積跨 Entry**

當新 `item_id` 觸發時，重啟 Web Speech 以清空累積：

```javascript
function onSpeechStarted(itemId) {
    if (itemId !== entryState.currentItemId) {
        speechRecognition.stop();  // onend 會自動 restart
    }
}
```

**問題 2：事件亂序（競爭條件）**

所有事件都用 `item_id` 找到對應的 Entry，不依賴「當前」狀態：

```javascript
// 正確做法：用 item_id 路由
function onTranscriptionCompleted(itemId, transcript) {
    const entry = entryState.entries.get(itemId);
    if (entry) entry.original = transcript;
}
```

**問題 3：response 事件沒有 item_id**

追蹤 `response_id → item_id` 對應：

```javascript
function onResponseCreated(responseId) {
    entryState.responseToItem.set(responseId, entryState.currentItemId);
}

function onTranslationDelta(responseId, delta) {
    const itemId = entryState.responseToItem.get(responseId);
    const entry = entryState.entries.get(itemId);
    if (entry) entry.translation += delta;
}
```

#### 4.7.5 驗收標準（分段 + 並行翻譯）

**智能分段測試**：

| 場景 | 預期結果 |
|------|----------|
| 對方說 3 句話（各 2-3 秒），停頓 300-500ms | 3 個獨立段落，各自有翻譯 |
| 對方連續說話 10 秒不停 | 自動分成 3-4 段（基於 25 字硬限制） |
| 對方語速很快（180 WPM） | 仍能合理分段（語法線索觸發） |
| 對方語速很慢（100 WPM） | 不會過早分段（延長停頓閾值） |
| 句子以 "right?" 或 "okay" 結尾 | 在該詞後分段 |

**並行翻譯測試**：

| 場景 | 預期結果 |
|------|----------|
| 單句 "Hello" | 1 個 Segment：Hello → 你好 |
| 兩句連續 "Hello" "World" | 2 個 Segment，各自完成翻譯 |
| 說話時新段落開始 | 舊段落繼續翻譯，新段落獨立處理 |
| 翻譯超時（>30 秒） | 顯示原文 + 錯誤狀態 |
| API 錯誤 | 當前 Segment 標記錯誤，其他不受影響 |
| 事件亂序到達 | 每個 Segment 仍正確對應（FIFO 隊列） |
| 同 item_id 多次 speech_started | 只有 1 個 Segment |

### 4.5 翻譯品質改良（2026-02-07 新增）

> **目標**：提升翻譯準確度，特別是領域術語和數字/金額。

#### 4.5.1 場景詞庫整合

**問題**：翻譯 API 不知道對話場景，可能誤譯領域術語。

**解決方案**：Prompt Injection — 在翻譯時注入詞庫提示

```python
# glossary.py
def get_glossary_hint(text: str, scenario: str) -> str:
    """找出文本中的詞庫術語，返回提示"""
    # 例：輸入 "direct debit", 場景 "bank"
    # 返回：'Key terms: "direct debit" = "直接付款授權"'
```

**詞庫內容**（`src/backend/domain_glossaries.json`）：
| 場景 | 術語數 | 範例 |
|------|--------|------|
| Bank | 34 | direct debit → 直接付款授權, sort code → 銀行分類代碼 |
| NHS | 32 | surgery → 診所（非手術）, GP → 家庭醫生 |
| Utilities | 30 | standing charge → 每日固定收費, meter reading → 錶讀數 |
| Insurance | 39 | excess → 自負額/墊底費, no claims bonus → 無索償折扣 |

#### 4.5.2 數字保持規則

**規則**：所有數字保持阿拉伯數字，不轉換成中文。

| 類型 | 英文 | 正確翻譯 | 錯誤翻譯 |
|------|------|---------|---------|
| 金額 | £500 | £500 | 五百英鎊 |
| 日期 | 15th March | 3月15日 | 三月十五日 |
| 時間 | 2:30pm | 下午2:30 | 下午兩點半 |
| 百分比 | 5% | 5% | 百分之五 |
| 電話 | 020 7123 4567 | 020 7123 4567 | — |

**原因**：
- 易於核對（用戶可快速比對英文原文）
- 避免轉換錯誤
- 簡化驗證邏輯

#### 4.5.3 翻譯驗證系統

**實現**：`src/frontend/translation_validator.js`

**驗證項目**：

| 驗證類型 | 檢測方法 | 嚴重程度 |
|---------|---------|---------|
| 數字缺失 | 提取源文和譯文數字，比對是否缺失 | High |
| 數字錯誤 | £500 → £50（數值不匹配） | High |
| 長度異常 | 譯文/源文長度比 < 0.2 或 > 4.0 | Medium |
| 未翻譯 | 中文輸出含大量英文單詞 | High |
| AI 失敗 | 含「我無法」「抱歉」等語句 | High |
| 無翻譯 | 源文 = 譯文 | High |

**UI 顯示**：
- 警告時：黃色邊框 + 「⚠️ 數字可能有誤，請對照英文原文」
- 正常時：綠色邊框（完成狀態）

---

## 5. 回應輔助模組（通話前準備 + 通話中 Quick Response）

> **⚠️ 重新設計（2026-02-05）**：原設計為「通話中打字輸入 → 生成講稿」，
> 經分析發現通話中打字不實用（5-10 秒沉默、無對話上下文），
> 重新定位為「通話前準備 + 通話中一鍵調用」。
> 詳見 `spec/lessons_learned.md` §5.3。

### 5.1 統一架構：兩種模式

**關鍵設計決策**：所有「回應輔助」功能（無論來源）都輸出到同一個 **Teleprompter** 組件。用戶只需看一個地方。

```
通話前（準備模式）               通話中（Quick Response 模式）
─────────────────              ────────────────────────────
[場景選擇]                      [已準備的講稿卡片]（來自 localStorage）
[詞彙預覽]                      [4 個快捷短語]（硬編碼）
[講稿生成 + 保存]               [🆘 Panic Button]（8 句拖延語）
     ↓                                ↓
     └───── 全部輸出到 Teleprompter 大字顯示 ─────┘
```

### 5.2 通話前準備畫面（Pre-Call Preparation Screen）

**Before**：通話中打字 → 等待生成 → 讀講稿（5-10 秒沉默，不實用）
**After**：通話前從容準備 → 保存為快捷卡片 → 通話中一鍵調用（實用）

**使用流程**：
```
1. 選場景（Bank / NHS / Utilities / Insurance）
2. 看場景詞彙和常用句（靜態數據）
3. 輸入中文：「我想查上個月的費用明細」（不急，慢慢打）
4. 生成英文講稿 + 備選方案（調用 POST /api/script/stream）
5. 保存為「快捷卡片」（localStorage）
6. 可準備多張卡片
7. 點「開始通話」→ 帶著準備好的卡片進入通話模式
```

**UI 設計**：
```
┌────────────────────────────────────────────────────────┐
│ 📋 通話前準備                                           │
├────────────────────────────────────────────────────────┤
│                                                        │
│  選擇場景：                                             │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐             │
│  │ 🏦   │  │ 🏥   │  │ 💡   │  │ 🛡️   │             │
│  │ 銀行  │  │ NHS  │  │ 水電  │  │ 保險  │             │
│  └──────┘  └──────┘  └──────┘  └──────┘             │
│                                                        │
│  📖 常用詞彙：account, balance, statement, charge...   │
│                                                        │
│  ✏️ 我想說：                                           │
│  ┌──────────────────────────────────────────────┐     │
│  │ 我想問一下上個月那筆 £50 的費用               │     │
│  └──────────────────────────────────────────────┘     │
│                                     [生成講稿]         │
│                                                        │
│  📜 生成結果：                                          │
│  ┌──────────────────────────────────────────────┐     │
│  │ I'd like to ask about the £50 charge from    │     │
│  │ last month. Could you explain what it's for? │     │
│  └──────────────────────────────────────────────┘     │
│  💡 替代：Can you tell me more about this fee?        │
│                                                        │
│  [💾 保存為快捷卡片]  [重新生成]                        │
│                                                        │
│  ── 已保存的卡片 ──                                    │
│  ┌────────────────┐  ┌────────────────┐              │
│  │ 查詢費用       │  │ 取消年費       │              │
│  │ I'd like to... │  │ I'd like to... │              │
│  │        [✕ 刪除]│  │        [✕ 刪除]│              │
│  └────────────────┘  └────────────────┘              │
│                                                        │
│           [▶ 開始通話（帶著 2 張卡片）]                 │
└────────────────────────────────────────────────────────┘
```

### 5.3 Quick Response Bar（通話中底部欄）

**取代現有的 textarea**，改為一排可點擊的按鈕：

```
┌──────────────────────────────────────────────────────┐
│ [查詢費用] [取消年費]                                  │  ← 已準備的講稿（localStorage）
│ [請再說一次] [請慢點說] [我確認一下] [謝謝再見]   [🆘] │  ← 4 個快捷短語 + Panic Button
└──────────────────────────────────────────────────────┘
```

- **上排**：用戶通話前準備的講稿卡片（來自 localStorage）
- **下排**：4 個預設快捷短語（硬編碼，無 API 調用，< 300ms）
- **右下角**：🆘 Panic Button（紅色，顯眼）
- 點擊任意按鈕 → **Teleprompter 大字顯示**

**交互規則**：
- 通話中不顯示 textarea（移除打字輸入）
- 所有回應輔助通過點擊完成（零打字）
- 每次點擊取代上一次顯示（Teleprompter 只顯示最新內容）

### 5.4 Panic Button 整合

點擊 🆘 Panic Button：
1. 立即在 Teleprompter 顯示一句拖延語（從 8 句中隨機選，不重複上一次）
2. 同時高亮用戶已準備的講稿卡片供選擇
3. **無 API 調用**，純本地數據，< 300ms 回應

**拖延語庫**（靜態數據，硬編碼）：
```javascript
const STALLING_PHRASES = [
  "Let me think about that for a moment...",
  "That's a good question. Give me a second...",
  "Could you hold on for just a second?",
  "I want to make sure I understand correctly...",
  "Let me just check something quickly...",
  "Hmm, let me consider that...",
  "I need a moment to think about this...",
  "That's interesting. Let me think..."
];
```

**UI 規格**：
- 顏色：紅色 (#dc3545)
- 大小：40x40px（Quick Response Bar 內）
- 觸發：單次點擊即觸發（不需按住）

### 5.5 Teleprompter 統一顯示組件

**所有回應輔助的統一輸出**。通話中用戶只需看這一個地方。

```
┌────────────────────────────────────────────────────┐
│                                                    │
│   I'd like to ask about the £50 charge from       │
│   last month. Could you explain what it's for?    │
│                                                    │
└────────────────────────────────────────────────────┘
```

**UI 規格**：
- 字體大小：28px（大字易讀）
- 行高：1.8（寬鬆間距）
- 最大寬度：600px（避免視線移動過大）
- 背景：淺色高對比（#f8f9fa）
- 自動 5 秒後淡出（或用戶手動關閉）
- 位置：翻譯區域上方或覆蓋顯示

**輸入來源**：
| 來源 | 觸發方式 | 延遲 |
|------|---------|------|
| 已準備的講稿 | 點擊卡片 | < 100ms |
| 快捷短語 | 點擊按鈕 | < 100ms |
| Panic 拖延語 | 點擊 🆘 | < 300ms |

### 5.6 API 規格

#### `POST /api/script/stream`

生成英文講稿供用戶照唸。支援空輸入（自動使用場景預設）。

**請求**：
```json
{
  "chinese_input": "我想問一下這筆費用是什麼",  // 可為空，使用場景預設
  "context": {
    "scenario": "bank",
    "conversation_history": [
      {"role": "them", "text": "I can see a £50 charge..."},
      {"role": "me", "text": "What is this charge for?"}
    ],
    "tone": "polite"
  }
}
```

**響應**（SSE 串流）：
```json
// 如果使用預設（chinese_input 為空）
{"type": "using_default", "prompt": "我想查詢帳戶餘額和最近的交易記錄"}
// 然後是正常的串流
{"type": "script_delta", "text": "I'd like to..."}
{"type": "script_done", "text": "I'd like to check my account balance..."}
{"type": "alternatives", "alternatives": ["Could you show me...", "I want to see..."]}
{"type": "done"}
```

#### `GET /api/script/scenarios`

返回所有場景的預設講稿選項。

**響應**：
```json
{
  "scenarios": {
    "bank": {
      "primary": "我想查詢帳戶餘額和最近的交易記錄",
      "options": [
        {"label": "查詢餘額", "prompt": "我想查詢帳戶餘額和最近的交易記錄"},
        {"label": "不明收費", "prompt": "我想詢問帳戶上一筆不明的收費是什麼"},
        ...
      ]
    },
    ...
  }
}
```

#### `GET /api/script/scenarios/{scenario}`

返回特定場景的預設講稿選項。

### 5.7 場景預設講稿

每個場景提供 5 個常用目的，用戶可一鍵選擇並生成講稿：

| 場景 | 常用目的 |
|------|----------|
| 銀行 | 查詢餘額、不明收費、更新資料、開戶咨詢、轉帳問題 |
| 醫療 | 預約 GP、領處方簽、轉診進度、檢驗結果、取消預約 |
| 水電 | 查帳單、更新付款、報讀數、換方案、搬家通知 |
| 保險 | 保障範圍、提出理賠、續約保費、更改資料、取消保單 |
| 一般 | 一般詢問、確認狀態、客服轉接、投訴反映、感謝結束 |

**UI 流程**：
1. 用戶選擇場景（如「銀行」）
2. 顯示「快速選擇常用目的」按鈕區
3. 用戶點擊按鈕（如「查詢餘額」）→ 自動生成講稿
4. 或直接點「生成講稿」（無需輸入）→ 使用場景主要預設

### 5.8 存儲設計

**localStorage 結構**：
```javascript
// Key: 'eca_prepared_scripts'
// Value: JSON array
[
  {
    "id": "script_1707100800000",
    "label": "查詢費用",           // 用戶自訂或自動摘要
    "scenario": "bank",
    "chineseInput": "我想問一下這筆費用是什麼",
    "englishScript": "I'd like to ask about this charge...",
    "alternatives": ["Can you tell me..."],
    "createdAt": 1707100800000
  }
]
```

---

## 6. Smart 建議模組

### 6.1 觸發邏輯（Smart Mode）

**問題**：不是每句話都需要建議，過多建議會干擾

**解決**：Smart 觸發 — 只在「可能需要回應」時顯示

```javascript
const shouldShowSuggestion = (transcript) => {
  const triggers = [
    // 問句
    /\?$/,
    /^(what|when|where|why|how|can|could|would|do|does|did|is|are|have|has)/i,
    // 要求確認
    /\b(correct|right|okay|agree|confirm)\?/i,
    // 等待回應
    /\b(what do you think|your thoughts|let me know)\b/i,
    // 沉默超過 3 秒
    transcript.silenceDuration > 3000
  ];

  return triggers.some(t =>
    t instanceof RegExp ? t.test(transcript.text) : t
  );
};
```

### 6.2 建議卡片設計

```
┌──────────────────────────────────────────────────────┐
│ 💬 建議回應：                                         │
│                                                      │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐    │
│  │"I wasn't   │  │"Can you    │  │"Is there   │    │
│  │ aware of   │  │ explain    │  │ any way to │    │
│  │ this fee"  │  │ what this  │  │ waive it?" │    │
│  │            │  │ is for?"   │  │            │    │
│  │ ────────── │  │ ────────── │  │ ────────── │    │
│  │ 我不知道    │  │ 可以解釋   │  │ 有辦法免除 │    │
│  │ 有這筆費用  │  │ 這是什麼嗎 │  │ 嗎？       │    │
│  │            │  │            │  │            │    │
│  │ [用這個]   │  │ [用這個]   │  │ [用這個]   │    │
│  └────────────┘  └────────────┘  └────────────┘    │
│                                                      │
│  ⏱️ 5 秒後自動隱藏                                   │
└──────────────────────────────────────────────────────┘
```

**UX 規格**：
- 最多顯示 3 個建議（減少選擇壓力）
- 5 秒後自動消失（非侵入式）
- 點擊「用這個」→ 複製到講稿區 + 顯示 Teleprompter
- 滑動手勢可提前隱藏

### 6.3 API 規格

#### `POST /api/suggest`

根據對話上下文生成回應建議。

**請求**：
```json
{
  "last_utterance": "That appears to be your annual fee.",
  "context": {
    "scenario": "bank",
    "goal": "dispute_charge",
    "conversation_history": [...]
  }
}
```

**響應**：
```json
{
  "suggestions": [
    {
      "english": "I wasn't aware of this fee. Can you explain what it covers?",
      "chinese": "我不知道有這筆費用。可以解釋一下包含什麼嗎？",
      "intent": "clarify"
    },
    {
      "english": "Is there any way to waive this fee?",
      "chinese": "有辦法免除這筆費用嗎？",
      "intent": "negotiate"
    },
    {
      "english": "I'd like to cancel this service if possible.",
      "chinese": "如果可以的話，我想取消這項服務。",
      "intent": "escalate"
    }
  ],
  "confidence": 0.85
}
```

---

## 7. Panic Button 設計

> **⚠️ 重新設計（2026-02-05）**：Panic Button 已整合到 Quick Response Bar（見 §5.4）。
> 不再是獨立浮動按鈕，而是 Quick Response Bar 的一部分。

### 7.1 功能定義

**問題**：用戶在通話中可能完全卡住，不知道說什麼

**解決**：Quick Response Bar 右下角紅色 🆘 按鈕，點擊後立即在 Teleprompter 顯示拖延語

### 7.2 行為流程（簡化版）

```
用戶點擊 🆘 Panic Button
        ↓
立即在 Teleprompter 顯示拖延語（< 300ms）
        ↓
同時高亮已準備的講稿卡片
        ↓
用戶選擇講稿或自己說
```

**注意**：MVP 階段不調用 API 生成建議、不播放 TTS。純本地操作確保 < 300ms。
Smart 建議（自動生成回應）將在 Phase 3（v1.5）實作。

### 7.3 "買時間" 短語庫

```javascript
const STALLING_PHRASES = [
  "Let me think about that for a moment...",
  "That's a good question. Give me a second...",
  "Hmm, let me consider that...",
  "I need a moment to think about this...",
  "Could you hold on for just a second?",
  "Let me just check something quickly...",
  "I want to make sure I understand correctly...",
  "That's interesting. Let me think..."
];
```

### 7.4 UI 規格

- 顏色：紅色 (#dc3545)
- 大小：40x40px（Quick Response Bar 內）
- 位置：Quick Response Bar 右下角
- 觸發：單次點擊即觸發

---

## 8. 快捷短語設計

> **⚠️ 重新設計（2026-02-05）**：快捷短語已整合到 Quick Response Bar（見 §5.3）。
> 不再是獨立組件，而是 Quick Response Bar 下排的按鈕。

### 8.1 預設短語

| 中文 | 英文 | 用途 |
|------|------|------|
| 請再說一次 | Could you repeat that, please? | 聽不清楚 |
| 請慢點說 | Could you speak more slowly? | 語速太快 |
| 我確認一下 | Let me confirm that... | 確認理解 |
| 謝謝再見 | Thank you. Goodbye. | 結束通話 |

### 8.2 UI 設計

快捷短語位於 Quick Response Bar 下排：

```
┌──────────────────────────────────────────────────────┐
│ [已準備的講稿1] [已準備的講稿2]                        │  ← 上排
│ [請再說一次] [請慢點說] [我確認一下] [謝謝再見]   [🆘] │  ← 下排
└──────────────────────────────────────────────────────┘
```

**規格**：
- 位置：通話中底部欄（取代 textarea）
- 樣式：圓角按鈕，可水平滾動
- 點擊：在 Teleprompter 大字顯示（< 100ms）
- TTS 播放：移至 Phase 3（v1.5）

---

## 9. 主介面設計

> **⚠️ 重新設計（2026-02-05）**：通話中介面移除 textarea，改用 Quick Response Bar。
> 新增通話前準備畫面。

### 9.1 通話前準備畫面（桌面版）

```
┌──────────────────────────────────────────────────────────────┐
│ 🎧 英文對話助手               [⚙️]              ⚪ 準備中     │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  選擇場景：                                                   │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐                   │
│  │ 🏦   │  │ 🏥   │  │ 💡   │  │ 🛡️   │                   │
│  │ 銀行  │  │ NHS  │  │ 水電  │  │ 保險  │                   │
│  └──────┘  └──────┘  └──────┘  └──────┘                   │
│                                                              │
│  📖 常用詞彙：account, balance, statement, charge...         │
│                                                              │
│  ✏️ 我想說（中文）：                                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 我想問一下上個月那筆 £50 的費用                       │   │
│  └──────────────────────────────────────────────────────┘   │
│                                           [生成講稿]         │
│                                                              │
│  📜 生成結果：                                                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ I'd like to ask about the £50 charge from last       │   │
│  │ month. Could you please explain what it's for?       │   │
│  └──────────────────────────────────────────────────────┘   │
│  💡 替代：Can you tell me more about this fee?              │
│                                                              │
│  [💾 保存為快捷卡片]  [重新生成]                              │
│                                                              │
│  ── 已保存的卡片（2 張）──                                   │
│  ┌────────────────┐  ┌────────────────┐                    │
│  │ 查詢費用       │  │ 取消年費       │                    │
│  │ I'd like to... │  │ I'd like to... │                    │
│  │        [✕ 刪除]│  │        [✕ 刪除]│                    │
│  └────────────────┘  └────────────────┘                    │
│                                                              │
│              [▶ 開始通話（帶著 2 張卡片）]                    │
└──────────────────────────────────────────────────────────────┘
```

### 9.2 通話中介面（桌面版）

```
┌──────────────────────────────────────────────────────────────┐
│ 🎧 英文對話助手               [⚙️] [📊]      🔴 通話中 3:42  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 📜 Teleprompter（點擊下方按鈕顯示）                   │   │
│  │                                                      │   │
│  │   I'd like to ask about the £50 charge from          │   │
│  │   last month. Could you explain what it's for?       │   │
│  │                                                      │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 🔊 對方說：                                 🟢 信心高  │   │
│  │                                                      │   │
│  │  "I can see there's a £50 charge from last month.   │   │
│  │   That appears to be your annual fee."              │   │
│  │                                                      │   │
│  │  ─────────────────────────────────────────────────  │   │
│  │  📝 我看到上個月有一筆 £50 的費用。                   │   │
│  │     那似乎是您的年費。                                │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────┐ ┌────────────┐                              │
│  │ 查詢費用   │ │ 取消年費   │           ← 已準備的講稿     │
│  └────────────┘ └────────────┘                              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    ┌─────────┐      │
│  │請再說│ │請慢點│ │我確認│ │謝謝  │    │ 🆘 求助 │      │
│  │一次  │ │說    │ │一下  │ │再見  │    └─────────┘      │
│  └──────┘ └──────┘ └──────┘ └──────┘    ← Quick Response  │
└──────────────────────────────────────────────────────────────┘
```

### 9.3 手機版（響應式）

```
┌────────────────────────┐
│ 🎧 英文助手      🔴 3:42│
├────────────────────────┤
│                        │
│ 📜 Teleprompter:       │
│ ┌────────────────────┐ │
│ │ I'd like to ask    │ │
│ │ about the £50      │ │
│ │ charge...          │ │
│ └────────────────────┘ │
│                        │
│ 🔊 對方說：       🟢    │
│ ┌────────────────────┐ │
│ │ "That appears to   │ │
│ │  be your annual    │ │
│ │  fee."             │ │
│ │ ─────────────────  │ │
│ │ 那似乎是您的年費    │ │
│ └────────────────────┘ │
│                        │
├────────────────────────┤
│ [查詢費用] [取消年費]   │
│ [請再說] [請慢] [🆘]   │
└────────────────────────┘
```

### 9.3 UI 設計原則

| 原則 | 說明 | 應用 |
|------|------|------|
| **Clarity Under Pressure** | 壓力下要簡潔清晰 | 大字體、高對比、單一焦點 |
| **Progressive Disclosure** | 漸進式揭示 | 預設只顯示翻譯+建議 |
| **Teleprompter Style** | 講稿像提詞器 | 大字、易讀、可滾動 |
| **Bilingual Clarity** | 雙語清晰區分 | 顏色/位置區分中英文 |

### 9.4 關鍵 UI 規格

| 元素 | 規格 | 原因 |
|------|------|------|
| 翻譯文字 | 24px, 高對比 | 壓力下快速閱讀 |
| 建議卡片 | 最多 3 個 | 減少選擇壓力 |
| Panic Button | 紅色, 55x55px | 緊急時易找到 |
| 快捷短語 | 4 個常用 | 拇指可及區域 |
| 信心指示 | 🟢🟡🔴 小圓點 | 非侵入式 |

---

## 10. 場景預設設計

### 10.1 核心場景

| 優先級 | 場景 | 頻率 | 嚴重性 | MVP |
|--------|------|------|--------|-----|
| **P0** | 銀行/金融電話 | 中 | 極高 | ✅ |
| **P0** | NHS/GP 預約 | 高 | 高 | ✅ |
| **P1** | 公用事業客服 | 高 | 中 | ✅ |
| **P1** | 保險 claim | 低 | 極高 | ✅ |
| **P2** | 租房/房東溝通 | 中 | 中 | v2 |
| **P2** | 學校/老師溝通 | 中 | 中 | v2 |

### 10.2 場景預設模板

```javascript
const scenarioPresets = {
  'bank': {
    name: '銀行/金融',
    commonTerms: ['account', 'balance', 'transaction', 'fraud', 'statement'],
    suggestedPhrases: [
      "I'd like to report a suspicious transaction",
      "Can you check my account balance?",
      "I need to dispute a charge"
    ],
    tone: 'formal'
  },
  'nhs': {
    name: 'NHS/GP',
    commonTerms: ['appointment', 'prescription', 'symptoms', 'referral'],
    suggestedPhrases: [
      "I'd like to book an appointment",
      "I need to renew my prescription",
      "Can I speak to a doctor?"
    ],
    tone: 'polite'
  },
  'utilities': {
    name: '公用事業',
    commonTerms: ['bill', 'meter reading', 'direct debit', 'tariff'],
    suggestedPhrases: [
      "I have a question about my bill",
      "I'd like to change my tariff",
      "Can I set up a payment plan?"
    ],
    tone: 'neutral'
  }
};
```

### 10.3 英國特有詞彙庫

| 場景 | 香港人可能不知道的詞彙 |
|------|----------------------|
| NHS | GP surgery, prescription charge, referral letter |
| Council | Council Tax, bin collection, housing benefit |
| 租房 | Tenancy agreement, deposit scheme, Section 21 |
| 銀行 | Sort code, standing order, direct debit |
| 保險 | Excess, no claims bonus, third party |

---

## 11. 後端 API 設計

### 11.1 API 端點總覽

| 端點 | 方法 | 功能 |
|------|------|------|
| `/api/token` | POST | 生成 Realtime API ephemeral token |
| `/api/script` | POST | 生成英文講稿 |
| `/api/suggest` | POST | 生成回應建議 |
| `/api/translate` | POST | 翻譯文字（備援） |

### 11.2 `/api/token`

生成 Realtime API 臨時憑證。

**請求**：
```json
{
  "voice": "marin"
}
```

**響應**：
```json
{
  "client_secret": "ek_1234...",
  "expires_at": 1234567890,
  "model": "gpt-realtime-mini-2025-12-15"
}
```

### 11.3 `/api/script`

生成英文講稿。

**請求**：
```json
{
  "chinese_input": "我想問一下這筆費用是什麼",
  "context": {
    "scenario": "bank",
    "conversation_history": [...],
    "tone": "polite"
  }
}
```

**響應**：
```json
{
  "english_script": "I'd like to ask about this charge...",
  "alternatives": ["Can you tell me...", "I'm wondering..."],
  "pronunciation_tips": [...]
}
```

### 11.4 `/api/suggest`

生成回應建議。

**請求**：
```json
{
  "last_utterance": "That appears to be your annual fee.",
  "context": {
    "scenario": "bank",
    "goal": "dispute_charge"
  }
}
```

**響應**：
```json
{
  "suggestions": [
    {"english": "...", "chinese": "...", "intent": "clarify"},
    {"english": "...", "chinese": "...", "intent": "negotiate"}
  ],
  "confidence": 0.85
}
```

---

## 12. 性能目標

### 12.1 即時翻譯性能（核心 KPI）

| 指標 | 目標 | 技術方案 | 備註 |
|------|------|---------|------|
| 英文即時顯示 | <100ms | Web Speech API（本地） | 用戶可立即看到對方說的英文 |
| 分段延遲 | <800ms | SmartSegmenter 混合策略 | 從停止說話到觸發分段 |
| 翻譯開始 | <500ms | Realtime API response.created | 句子結束後開始翻譯 |
| 翻譯完成 | <1.5s | response.output_text.done | 從句子結束到翻譯完成 |
| 翻譯串流 | 即時 | response.output_text.delta | 逐字顯示翻譯結果 |

### 12.2 並行處理性能

| 指標 | 目標 | 技術方案 |
|------|------|---------|
| 並行段落數 | 無限制 | Promise 並行 + SegmentStore |
| 段落隔離 | 100% | 每個 Segment 獨立生命週期 |
| 翻譯錯配率 | 0% | 雙向索引 + FIFO 隊列 |
| 超時處理 | <30s | 超時後顯示原文 |

### 12.3 其他性能指標

| 指標 | 目標 | 技術方案 |
|------|------|---------|
| 講稿生成延遲 | <1.5s | gpt-5-mini + 串流輸出 |
| 建議生成延遲 | <1s | 預生成 + 快取 |
| 連線建立 | <1s | WebRTC 優化 |
| 首次可用時間 | <3s | 預載入 + 懶載入 |

---

## 13. 成本估算

### 13.1 API 成本

| 使用模式 | 月用量 | 估算成本 |
|---------|--------|---------|
| 輕度用戶 | 30 分鐘/月 | ~$2 |
| 中度用戶 | 120 分鐘/月 | ~$8 |
| 重度用戶 | 300 分鐘/月 | ~$20 |

### 13.2 定價建議

| 方案 | 價格 | 包含 |
|------|------|------|
| **Free** | £0 | 30 分鐘/月 |
| **Basic** | £9.99/月 | 150 分鐘/月 |
| **Pro** | £19.99/月 | 無限制 + 對話記錄 |
| **Family** | £29.99/月 | 4 人共用 |

---

## 14. 未來願景

### 14.1 產品路線圖

```
2026 Q1: MVP ─────────────────────────────────────
├── 即時翻譯（英→中）
├── 講稿生成（中→英）
├── Panic Button
└── 快捷短語

2026 Q2: v1.5 ────────────────────────────────────
├── Smart 建議
├── 場景預設
├── 對話記錄
└── 桌面應用 (Tauri)

2026 Q3: v2.0 ────────────────────────────────────
├── 手機應用
├── Whisper Mode（耳機模式）
├── 信心指示
└── 學習功能

2027: v3.0 ───────────────────────────────────────
├── AR 眼鏡整合
├── 離線模式
└── 語音克隆（用自己聲音說英文）
```

### 14.2 長期願景

> **「讓語言障礙從此消失」**

- 從英文對話 → 任何語言對話
- 從電話場景 → 任何對話場景
- 從輔助工具 → 隱形的語言橋樑

---

## 15. 風險與緩解

| 風險 | 影響 | 機率 | 緩解措施 |
|------|------|------|---------|
| 翻譯準確度不足 | 用戶誤解 | 中 | 顯示原文 + 信心指示 |
| 延遲過高 | UX 差 | 中 | 多供應商備援 |
| 用戶不習慣「照唸」 | 不自然 | 中 | 可調整建議風格 |
| 競品快速跟進 | 市場份額 | 中 | 專注香港移民細分市場 |
| 成本超支 | 定價壓力 | 低 | 混合架構 + 本地模型 |

---

## 16. 參考資料

### 16.1 智能分段與並行翻譯研究
- `spec/design_parallel_translation.md` - 並行翻譯架構與 SegmentStore 設計（詳細實現規格）
- `spec/research/speech_segmentation.md` - 人類說話習慣與 SmartSegmenter 設計
- `spec/research/webspeech_capabilities.md` - Web Speech API 能力與限制分析
- `spec/research/realtime_api_research.md` - OpenAI Realtime API 能力測試

### 16.2 設計研究文件
- `spec/research/design_sprint/competitor_analysis.md` - 競品分析
- `spec/research/design_sprint/user_research.md` - 用戶研究
- `spec/research/design_sprint/ux_innovation.md` - UX 創新
- `spec/research/design_sprint/technical_research.md` - 技術研究
- `spec/research/design_sprint/final_design_synthesis.md` - 最終整合方案

### 16.3 外部參考
- [MDN Web Speech API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API) - Web Speech API 官方文檔
- [OpenAI Realtime VAD Guide](https://platform.openai.com/docs/guides/realtime-vad) - OpenAI 語音活動檢測
- [VirtualSpeech Speaking Rate](https://virtualspeech.com/blog/average-speaking-rate-words-per-minute) - 語速研究
- [BYU Pause Duration Study](https://scholarsarchive.byu.edu/cgi/viewcontent.cgi?article=11044&context=etd) - 停頓時長研究

### 16.4 過時文件（僅供參考，位於 `spec/_archive/`）
- `_archive/role_templates_v2.md` - [DEPRECATED] 舊版 Role Templates
- `_archive/pivot_design_v2.md` - 舊版轉型設計（已整合到本文件）
- `_archive/critical_requirements_v3.md` - 關鍵需求定義（已整合到 4.2-4.3 節）

---

*最後更新：2026-02-07*
*版本：2.8*
*狀態：implemented*

### 實現狀態

| 組件 | 狀態 | 實現位置 |
|------|------|---------|
| 即時翻譯（方案 A） | ✅ 已實現 | `eca_parallel_test.html` |
| 通話前準備畫面 | ✅ 已實現 | `eca_parallel_test.html` |
| Quick Response Bar | ✅ 已實現 | `eca_parallel_test.html` |
| Teleprompter Overlay | ✅ 已實現 | `eca_parallel_test.html` |
| Panic Button | ✅ 已實現 | `eca_parallel_test.html` |
| 講稿生成 API | ✅ 已實現 | `script_generator.py` |
| 場景預設講稿 | ✅ 已實現 | `script_generator.py`, `eca_parallel_test.html` |
| 翻譯 API | ✅ 已實現 | `main.py /api/translate/stream` |

### 更新日誌

| 版本 | 日期 | 變更 |
|------|------|------|
| 2.8 | 2026-02-07 | **場景預設講稿**：§5.6 新增場景預設 API、§5.7 場景預設講稿設計、每個場景 5 個常用目的一鍵生成 |
| 2.7 | 2026-02-06 | **MVP 實現完成**：新增實現狀態表；狀態改為 `implemented` |
| 2.6 | 2026-02-05 | **§5 重新設計**：講稿生成改為「通話前準備模式 + Quick Response Bar」；§7 Panic Button 整合到 Quick Response Bar；§8 快捷短語整合到 Quick Response Bar；§9 主介面更新（新增準備畫面、通話中移除 textarea） |
| 2.5 | 2026-02-03 | 新增：gpt-4.1-nano 用於即時翻譯（703ms）；SmartSegmenter 5 種預設模式（預設為「快速」）；動態穩定性檢測 |
| 2.4 | 2026-02-02 | 採用方案 A：Web Speech + 後端 API 翻譯（避免 OpenAI 對話模式） |
| 2.3 | 2026-02-02 | 澄清：雙軌架構（Web Speech API 必須用於即時英文預覽，OpenAI 僅用於最終記錄+翻譯） |
| 2.2 | 2026-02-01 | 修復：重複 4.4 章節、更新參考資料路徑（整理後目錄結構） |
| 2.1 | 2026-02-01 | 新增：智能分段系統 (4.2)、並行翻譯架構 (4.3)、性能指標更新 (12.1-12.2) |
| 2.0 | 2026-01-29 | 初始 ECA 設計 |
